name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Validate manual-versions.json
        run: node test/validate-manual-versions.js
      - name: Validate ci-versions.json
        run: node test/validate-manual-versions.js ci-versions.json
      - name: Run tests
        run: node test/test.js
